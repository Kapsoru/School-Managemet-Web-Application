@model Prescolaire.Models.Absence

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Absence</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="AbsenceId" />
            <div class="form-group">
                <label asp-for="StudentId" class="control-label"></label>
                <select asp-for="StudentId" class="form-control" asp-items="ViewBag.StudentId"></select>
                <span asp-validation-for="StudentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AbsenceDate" class="control-label"></label>
                <input asp-for="AbsenceDate" class="form-control" />
                <span asp-validation-for="AbsenceDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="form-group">
                    <label asp-for="SchoolYearId" class="control-label"></label>
                    <select asp-for="SchoolYearId" class="form-control" asp-items="ViewBag.SchoolYearId">
                    </select>
                    <span asp-validation-for="SchoolYearId" class="text-danger"></span>
                </div>
                <span asp-validation-for="SchoolYear" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
